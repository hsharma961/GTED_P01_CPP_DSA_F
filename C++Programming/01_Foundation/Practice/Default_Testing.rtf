#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<string> words;
    vector<string> disliked = {"bad"};
    string temp;

        cout << "enter words carefully\n";
        while(cin >> temp) {
            words.push_back(temp);
        }
            cout << "Number of words:" << words.size() << '\n';
        
        for (const string& word:words){
            bool bw = false;

            for (const string& is_bad:disliked){
                if (word==is_bad){
                bool bw = true;
                break;
                }
            }
                                         
        if(bw) {
            cout << "BLOOP\n";
        }else{ 
            cout << word;
        }
        }
    system("pause");

    return 0;
}