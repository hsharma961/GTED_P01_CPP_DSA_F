#include <iostream>
using namespace std;

// making random funtions
int add (int x){
    x+x + (x*x);
    return x+x + (x*x);
}
int main()
{
    for (int i = 0; i<100; ++i)
        cout << i << '\t' << add(i)<< '\n';
        
    system("pause");
    return 0;
}