#include <iostream>
#include <vector>
#include <string>

int main(){

    vector<string>words;
    vector<string>disliked = {'fat','ugly', 'bad'};
    bool bad;

    for(string temp; cin>>temp;)
      words.push_back(temp)
      bool bad=0;
      
    if(temp = disliked){
      bool bad = 1;
      cout<<"BLEEP" <<'\n';
    }
      cout<<"words" <<'\n';


  system("pause")
  return 0;
}